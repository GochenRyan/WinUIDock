<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dock.WinUI3.Controls">

    <Style TargetType="local:DocumentDockControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:DocumentDockControl">
                    <!-- IsSizeTracked=False: the dock's visible bounds are written
                         by the CONTENT host inside DocumentControl (stored-size
                         invariant: bounds = content DIPs); this pane wrapper
                         would race it with the whole-pane rect. -->
                    <local:DockableControl TrackingMode="Visible"
                                           IsSizeTracked="False">
                        <local:DocumentControl x:Name="PART_DocumentControl"/>
                    </local:DockableControl>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
